namespace CardSuitExplorer
{
    // Enum that represents the four suits in a deck of cards.
    // By default, enum values start from 0 and increase by 1.
    public enum CardSuits
    {
        Clubs,      // 0
        Diamonds,   // 1
        Hearts,     // 2
        Spades      // 3
    }
}